{
  "name": "mashbean-vp-verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OIDC4VP verifier (VP host) for Taiwan TWDIW SD-JWT-VC, on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@sd-jwt/core": "^0.10.0",
    "@sd-jwt/sd-jwt-vc": "^0.10.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.80.0"
  }
}
